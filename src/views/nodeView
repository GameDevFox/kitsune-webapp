<div class="well well-sm">
	 <div class="ids">
	 	 <div v-for="name in names" class="name label label-primary">{{ name }}</div>
	 	 <div class="node label label-default">{{ node }}</div>
	 </div>
	 <div v-if="types.length != 0" class="types">
	 	 [<div v-for="type in types" class="type">{{ type }}</div>]
	 </div>
	 <div v-for="head in heads" class="head">{{ head }}</div>
	 <div v-for="tail in tails" class="tail">{{ tail }}</div>
	 <div class="string">{{ string }}</div>
</div>
